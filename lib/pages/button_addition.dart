import 'package:flutter/material.dart';

class ButtonAdittion extends StatelessWidget {
  const ButtonAdittion({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: (){

      }, child: const Text('I`m a buttom'));
  }
}
